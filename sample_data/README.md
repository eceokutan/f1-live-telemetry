Sample data is optional and primarily for local demos/import testing.

Notes:
- Large telemetry CSV files are not tracked in Git by default.
- If `data/csv_importer.py` cannot find telemetry files, it skips those sessions.
- You can populate this directory with your own exported telemetry CSVs.
