# F1 Telemetry Dashboard - Dependencies
# Supports Assetto Corsa and AI Race Engineering

# =============================================================================
# UI Framework
# =============================================================================
PyQt5>=5.15.0
matplotlib>=3.5.0
numpy>=1.21.0

# =============================================================================
# Telemetry Backend
# =============================================================================

# =============================================================================
# AI Race Engineer (Optional - Hugging Face LLM + local rules)
# =============================================================================
# Data validation
pydantic>=2.5.0

# Hugging Face Inference & Local LLM
huggingface-hub>=0.23.0
hf_xet>=1.1.0
llama-cpp-python>=0.2.50  # GGUF model inference (replaces transformers+peft at runtime)
# transformers>=4.36.0  # Conversion only — install via requirements-convert.txt
# peft>=0.10.0          # Conversion only — install via requirements-convert.txt

# Utilities
tenacity>=8.2.0
python-dotenv>=1.0.0

# =============================================================================
# Voice Input/Output (webrtcvad + faster-whisper + Kokoro TTS)
# =============================================================================
pyaudio>=0.2.13
webrtcvad>=2.0.10
faster-whisper>=1.0.0
pykokoro>=0.6.5

# =============================================================================
# Push-to-Talk (hold V key or joystick button to talk)
# =============================================================================
pynput>=1.7.6           # Global keyboard hooks (works when game has focus)
# pygame>=2.5.0         # Optional: joystick/gamepad PTT button (pip install pygame)

# =============================================================================
# Optional: LiveKit (Future - not currently used)
# =============================================================================
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# livekit>=0.9.0
# livekit-api>=0.4.0
# httpx>=0.25.0
# pyyaml>=6.0.0
