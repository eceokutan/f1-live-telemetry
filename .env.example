# Local LLM (llama-cpp-python + GGUF)
# Generate model: python scripts/convert_to_gguf.py --llama-cpp-path /path/to/llama.cpp
# LOCAL_MODEL_PATH=race_engineer_gguf/granite-race-engineer-Q4_K_M.gguf
# LOCAL_LLM_N_GPU_LAYERS=0   # 0=CPU, -1=full GPU offload
# LOCAL_LLM_N_THREADS=4      # physical CPU cores (auto-detected if unset)

# Optional: Kokoro Text-to-Speech (for AI voice output, local model)
# KOKORO_VOICE=bm_lewis
# Note: Kokoro model/voice files auto-download and cache on first run.
# Optional: run background model prewarm at app startup (default: true)
# PREWARM_MODELS_ON_START=true
# Note: Speech-to-Text and Text-to-Speech run locally (no API key needed)

# Optional: LiveKit (for future WebRTC integration)
# LIVEKIT_URL=wss://your-livekit-instance.livekit.cloud
# LIVEKIT_API_KEY=your_livekit_api_key_here
# LIVEKIT_API_SECRET=your_livekit_api_secret_here

# Optional: LangSmith (for LLM observability)
# LANGSMITH_API_KEY=your_langsmith_api_key_here

# Application Settings
LOG_LEVEL=INFO
DEBUG=False

# Jarvis Post (post-race analysis) - separate pipeline
# POSTRACE_HF_API_TOKEN=hf_your_token_here
# POSTRACE_HF_MODEL_ID=your_model_id
# POSTRACE_HF_SPACE_URL=https://your-space.hf.space
